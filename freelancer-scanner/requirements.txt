# Grundgeruest
fastapi>=0.115
uvicorn[standard]>=0.32
jinja2>=3.1
python-multipart>=0.0.9

# Konfiguration und Validierung
pydantic>=2.9
pydantic-settings>=2.6
PyYAML>=6.0

# Datenbank (SQLite ist in Python enthalten)
SQLAlchemy>=2.0

# HTTP und Zeitsteuerung
httpx>=0.27
APScheduler>=3.10

# Tests
pytest>=8.3

# --- Optional: nur noetig, wenn DEMO_MODE=false ---
# Je nach LLM_PROVIDER wird genau eines der beiden gebraucht.
anthropic>=0.40
# openai>=1.50

# --- Optional: nur fuer PostgreSQL / Supabase statt SQLite ---
# psycopg[binary]>=3.2
